services:
  db :
    image: postgres:16
    environment:
      POSTGRES_USER : admin
      POSTGRES_PASSWORD : admin123
      POSTGRES_DB : prediction_history
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:

